interface:
  display_name: "Create PR"
  short_description: "Commit, push, and open a pull request"
  default_prompt: "Use $create-pr to prepare a pull request from the current branch. If code changed, run $code-change-verification first and include its fresh verification summary in the PR."
