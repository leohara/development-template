interface:
  display_name: "Code Change Verification"
  short_description: "Verify code changes in the task worktree"
  default_prompt: "Use $code-change-verification in the task worktree that contains the diff. If the task has not been isolated yet, use $using-git-worktrees first, then hand the fresh verification summary to $create-pr."
