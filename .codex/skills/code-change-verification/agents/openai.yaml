interface:
  display_name: "Code Change Verification"
  short_description: "Verify repository code changes"
  default_prompt: "Use $code-change-verification to run this repository's verification stack. By default, run pnpm run lint, pnpm run type-check, pnpm run test, and pnpm run build, then report any failures or environment blockers."
